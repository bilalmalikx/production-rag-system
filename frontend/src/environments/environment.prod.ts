export const environment = {
  production: true,
  apiUrl: 'https://your-backend-api.com', // Production URL
  apiTimeout: 30000,
  maxFileSize: 50 * 1024 * 1024,
  allowedFileTypes: ['application/pdf']
};